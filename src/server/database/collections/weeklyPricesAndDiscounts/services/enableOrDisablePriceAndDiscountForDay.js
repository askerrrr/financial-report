var createQuery = (disableStatus, nmID) => {
  var query = {};
  var arrayFilters = [];
  var queryKey;
  var queryValue;

  if (disableStatus) {
  }
};

var enableOrDisablePriceAndDiscountForDay = async (collection, userId, disableStatus, nmID) => {};

module.exports = enableOrDisablePriceAndDiscountForDay;
